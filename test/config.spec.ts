/// <reference path="../typings/index.d.ts" />

import { expect } from 'chai';

describe('My suite', () => {
  it('shoudl work', () => {
    //console.log("yes!");
    expect(true).to.be.equal(true);
  })
})
